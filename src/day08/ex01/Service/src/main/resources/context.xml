<beans xmlns="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:context="http://www.springframework.org/schema/context"
  xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

  <context:property-placeholder location="classpath:db.properties"/>

  <bean id="hikConfig" class="com.zaxxer.hikari.HikariConfig">
    <property name="jdbcUrl" value="${db.url}"/>
    <property name="username" value="${db.user}"/>
    <property name="password" value="${db.password}"/>
  </bean>

  <bean id="hikDataSource" class="com.zaxxer.hikari.HikariDataSource">
    <constructor-arg ref="hikConfig"/>
  </bean>

  <bean id="driverManagerDs" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
    <property name="url" value="${db.url}"/>
    <property name="username" value="${db.user}"/>
    <property name="password" value="${db.password}"/>
    <property name="driverClassName" value="${db.driver.name}"/>
  </bean>

  <bean id="usersRepositoryJdbcImpl" class="school21.spring.service.repositories.UsersRepositoryJdbcImpl">
    <constructor-arg ref="hikDataSource" />
  </bean>

  <bean id="usersRepositoryJdbcTemplateImpl" class="school21.spring.service.repositories.UsersRepositoryJdbcTemplateImpl">
    <constructor-arg ref="driverManagerDs" />
  </bean>
</beans>
